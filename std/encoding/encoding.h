// SafeC Standard Library — Encoding Master Header
// ASCII validation/case conversion, UTF-8 encode/decode, URL (percent)
// encoding, and Base64. Include std/encoding/*.sc for implementations
// (each header pairs 1:1 with a same-named .sc, same convention as the
// rest of std/ — see std/serial/json.h's top comment).
#pragma once
#include <std/encoding/ascii.h>
#include <std/encoding/utf8.h>
#include <std/encoding/url.h>
#include <std/encoding/base64.h>
